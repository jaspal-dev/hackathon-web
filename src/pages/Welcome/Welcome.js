import { Typography } from "@mui/material";

const Welcome = () => {
  return (
    <Typography variant={"h1"} mt={35} textAlign={"center"}>
      Welcome
    </Typography>
  );
};

export default Welcome;
